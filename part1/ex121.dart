// Write a Java program to reverse a given linked list.
// Example: For linked list 20->40->60->80, the
//reversed linked list is 80->60->40->20

void main() {
  var numbers = [20, 40, 60, 80];

  print(numbers.reversed.toList());
}
